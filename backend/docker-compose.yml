version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - PGHOST=host.docker.internal
      - PGPORT=5433
      - PGDATABASE=biblioteca
      - PGUSER=postgres
      - PGPASSWORD=2311
    restart: unless-stopped

volumes:
  postgres_data:
